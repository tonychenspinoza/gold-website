# 如何在手机上查看网站

## 网站已支持移动端响应式设计 ✅

网站已经做了完整的移动端适配，包括：
- 响应式导航菜单（移动端汉堡菜单）
- 移动端优化的布局和间距
- 触摸友好的按钮和链接
- 响应式图片和卡片

## 查看方法

### 方法1：在电脑浏览器中模拟手机（推荐用于快速预览）

1. 打开 Chrome 浏览器
2. 按 `F12` 打开开发者工具
3. 点击工具栏上的**设备工具栏图标**（或按 `Ctrl+Shift+M`）
4. 选择手机型号（如 iPhone 12, Samsung Galaxy 等）
5. 刷新页面查看移动端效果

### 方法2：在真实手机上查看

1. **确保开发服务器正在运行**
   ```bash
   cd gold-website
   npm run dev
   ```

2. **找到您的电脑IP地址**
   - Windows: 打开命令提示符，输入 `ipconfig`，找到 "IPv4 地址"
   - 通常是 `192.168.x.x` 或 `10.0.x.x`

3. **在手机上访问**
   - 确保手机和电脑连接到**同一个WiFi网络**
   - 在手机浏览器中输入：`http://您的IP地址:3000`
   - 例如：`http://192.168.1.100:3000`

4. **如果无法访问，可能需要：**
   - 修改 `package.json` 中的 dev 脚本：
     ```json
     "dev": "next dev -H 0.0.0.0"
     ```
   - 或者检查防火墙设置，允许端口3000的访问

### 方法3：使用ngrok等工具（适合远程测试）

如果需要让不在同一网络的设备访问，可以使用ngrok等工具创建公网隧道。

## 移动端特性

- ✅ 响应式导航（汉堡菜单）
- ✅ 触摸优化的按钮大小
- ✅ 单列布局（产品列表、证书等）
- ✅ 移动端友好的字体大小
- ✅ 优化的图片加载
- ✅ WhatsApp浮动按钮

