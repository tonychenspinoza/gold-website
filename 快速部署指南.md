# 🚀 快速部署指南 - 将网站部署到测试网址

## 方式一：使用 Vercel 网页版（推荐，最简单）⭐

### 步骤 1: 准备代码
1. 确保代码已提交到 Git（可选，也可以直接上传）

### 步骤 2: 访问 Vercel
1. 打开浏览器，访问：**https://vercel.com**
2. 点击右上角 **"Sign Up"** 或 **"Log In"**
3. 使用 **GitHub**、**GitLab** 或 **Bitbucket** 账号登录（推荐 GitHub）

### 步骤 3: 创建新项目
1. 登录后，点击 **"Add New Project"** 或 **"New Project"**
2. 如果代码在 GitHub：
   - 选择你的仓库
   - 点击 **"Import"**
3. 如果代码不在 GitHub（直接上传）：
   - 点击 **"Deploy"** 标签
   - 拖拽整个 `gold-website` 文件夹到页面上
   - 或点击 **"Browse"** 选择文件夹

### 步骤 4: 配置项目
1. **Framework Preset**: 选择 **Next.js**（通常会自动检测）
2. **Root Directory**: 如果项目在子目录，填写 `gold-website`，否则留空
3. **Build Command**: `npm run build`（默认）
4. **Output Directory**: `.next`（默认，自动）
5. **Install Command**: `npm install`（默认）

### 步骤 5: 部署
1. 点击 **"Deploy"** 按钮
2. 等待 2-3 分钟，Vercel 会自动：
   - 安装依赖
   - 构建项目
   - 部署到云端

### 步骤 6: 获取测试网址
部署完成后，你会看到：
- ✅ **生产环境网址**：`https://your-project-name.vercel.app`
- ✅ **预览网址**：每次推送代码都会生成新的预览网址

**这个网址就是你的测试网址，可以直接分享给别人！** 🎉

---

## 方式二：使用 Vercel CLI（需要命令行）

### 步骤 1: 登录 Vercel
```bash
vercel login
```
这会打开浏览器，授权后即可登录。

### 步骤 2: 部署项目
```bash
vercel --yes
```

### 步骤 3: 获取网址
部署完成后，CLI 会显示你的测试网址。

---

## 📝 部署后的操作

### 查看部署状态
- 访问 **https://vercel.com/dashboard** 查看所有项目
- 点击项目可以查看部署历史、日志等

### 更新网站内容
1. **如果使用 GitHub**：
   - 修改代码后，推送到 GitHub
   - Vercel 会自动重新部署

2. **如果直接上传**：
   - 在 Vercel 项目页面点击 **"Redeploy"**
   - 或重新上传代码

### 自定义域名（可选）
1. 在 Vercel 项目设置中，进入 **"Domains"**
2. 添加你的域名（如：`test.yourdomain.com`）
3. 按照提示配置 DNS 记录

---

## 🌐 其他免费部署平台（备选）

### Netlify
1. 访问 **https://www.netlify.com**
2. 注册/登录账号
3. 拖拽项目文件夹或连接 GitHub
4. 自动部署，获得 `your-project.netlify.app` 网址

### Railway
1. 访问 **https://railway.app**
2. 注册/登录账号
3. 创建新项目，连接 GitHub 或上传代码
4. 自动部署，获得 `your-project.up.railway.app` 网址

### Render
1. 访问 **https://render.com**
2. 注册/登录账号
3. 创建 Web Service，连接 GitHub
4. 自动部署，获得 `your-project.onrender.com` 网址

---

## ⚠️ 注意事项

1. **免费套餐限制**：
   - Vercel 免费套餐：无限项目，每月 100GB 带宽
   - 适合测试和小型项目

2. **环境变量**（如果需要）：
   - 在 Vercel 项目设置中添加环境变量
   - 例如：`NEXT_PUBLIC_BASE_URL`

3. **构建时间**：
   - 首次部署可能需要 3-5 分钟
   - 后续更新通常 1-2 分钟

---

## 🎯 推荐流程

**最快方式**：
1. 访问 **https://vercel.com**
2. 使用 GitHub 登录
3. 点击 **"Add New Project"**
4. 拖拽 `gold-website` 文件夹
5. 点击 **"Deploy"**
6. 等待完成，获得测试网址

**总耗时**：约 5 分钟 ⚡

---

## 📞 需要帮助？

如果遇到问题：
1. 检查构建日志（Vercel 会显示详细错误）
2. 确保 `package.json` 中的脚本正确
3. 检查 Node.js 版本（需要 18+）

部署完成后，你的测试网址格式类似：
- `https://sing-chen-gold-website.vercel.app`
- `https://gold-website-abc123.vercel.app`

**这个网址可以直接分享给任何人查看！** 🚀

