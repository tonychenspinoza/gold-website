# 🔧 修复 Vercel 部署错误

## 问题
Vercel 构建失败，错误：`page.tsx doesn't have a root layout`

## 原因
Next.js 14 要求 `viewport` 必须单独导出，不能放在 `metadata` 中。

## 修复方法

### 方法一：在 GitHub 网页上直接修复（推荐，最快）

1. 访问：https://github.com/tonychenspinoza/gold-website
2. 进入文件：`src/app/layout.tsx`
3. 点击右上角的 **编辑按钮**（铅笔图标）
4. 将整个文件内容替换为：

```typescript
import type { Metadata, Viewport } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'SING CHEN TRADE LIMITED - 香港黃金貿易公司',
  description: 'SING CHEN TRADE LIMITED 為香港註冊之黃金貿易公司，提供金條、22K 黃金首飾及精品黃金製品的展示與線下交易服務。',
}

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 5,
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="zh-HK">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
```

5. 滚动到底部，填写提交信息：
   - Commit message: `fix: separate viewport from metadata for Next.js 14`
   - 选择 "Commit directly to the main branch"
6. 点击 **"Commit changes"**
7. Vercel 会自动检测到更改并重新部署（等待 2-3 分钟）

### 方法二：使用 Git 命令行

如果你有 `tonychenspinoza/gold-website` 仓库的访问权限：

```bash
# 1. 克隆正确的仓库（如果还没有）
git clone https://github.com/tonychenspinoza/gold-website.git
cd gold-website

# 2. 修复 layout.tsx 文件（使用上面的代码）

# 3. 提交并推送
git add src/app/layout.tsx
git commit -m "fix: separate viewport from metadata for Next.js 14"
git push origin main
```

## 修复后的效果

- ✅ 构建成功
- ✅ 网站正常部署
- ✅ 可以访问测试网址

## 验证

修复后，Vercel 会自动重新部署。在 Vercel 控制台查看：
- 构建日志应该显示 "✓ Compiled successfully"
- 部署状态变为 "Ready"
- 获得可访问的测试网址

